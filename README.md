# Practive_CV_Learn

这是长安大学机器人工程系《数字图像处理与机器学习》课程实验的 OpenCV 实现版本。

## Usage

本项目使用 `Xmake` 托管，在 Ubuntu 24.04 下开发。

需要按照 [Xmake 官方网站](https://xmake.io/zh/) 的提示安装 Xmake 环境。

进入包含 `xmake.lua` 文件的项目目录，运行：
```bash
xmake # 下载所依赖的库文件并编译；
xmake run chapter<x> # 运行项目 Chapter x
```
